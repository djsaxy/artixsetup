# artixsetup
Setup Artix from new install using https://github.com/feribsd/artix-install

Very much a work in progress - lots of the scripts will not work right now

Readme is a to do list
- Set up basic packages (vim, ranger, fastfetch)
- Set up doas, ufw, ssh
- Prompts for tailscale, syncthing, windowmanager (and move dots to .config)
- pause on errors
- touchpad name variable for xinput in xinitrc
- num lock on startup
- add power profile manager after comparing options
- dinit audio requires turnstile, which requires reboot before enabling pipewire, pipewire-pulse, wireplumber
